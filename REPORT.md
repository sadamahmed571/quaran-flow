# تقرير مراجعة شاملة — مصحف خطى

> تاريخ المراجعة: سبتمبر 2026
> المراجع: Antigravity AI
> الإصدار الحالي: v27 (Service Worker)

---

## نظرة عامة على التطبيق

مصحف خطى تطبيق ويب تقدمي (PWA) متكامل لقراءة القرآن الكريم وتدبره، يتميز بـ:

- قراءة المصحف بتصميم مستوحى من المصاحف المطبوعة (604 صفحة)
- تفسير الميسر مدمج ومخزن محلياً
- تبيان AI — مساعد ذكي مبني على Gemini 2.5 Flash
- بحث سريع في الآيات والسور
- مفضلة وإشارة مرجعية للآيات
- مشغل صوتي مع خيارات قراء متعددين
- خطة يومية لتتبع الورد اليومي
- ثلاثة ثيمات: مصحفي (كريمي)، ليلي، جنة (أخضر)

---

## الهيكل المعماري

```
الجذر/
├── index.html          ← الصفحة الرئيسية (2946 سطر — كل CSS+JS فيه)
├── quran.html          ← صفحة القراءة
├── css/
│   ├── quran.css       ← تصميم صفحة القراءة (2777 سطر)
│   └── fonts-quran.css ← خطوط المصحف
├── js/
│   ├── quran.js        ← المنطق الرئيسي (1596 سطر)
│   ├── AudioPlayer.js  ← مشغل الصوت
│   ├── AudioService.js ← خدمة الصوت (URLs)
│   └── db.js           ← IndexedDB wrapper
├── assets/
│   ├── quran.json      ← بيانات المصحف (2.5MB)
│   └── tafseerMouaser_v03.json ← التفسير الميسر (5.5MB)
├── server/app.ts       ← Express + Gemini API routes
├── sw.js               ← Service Worker (v27)
└── manifest.json       ← PWA manifest
```

---

## نقاط القوة

### في الكود
- حماية XSS ممتازة: استخدام textContent بدلاً من innerHTML في بناء DOM ديناميكي، ودالة sanitizeHtml() متكاملة
- IndexedDB ذكية: تخزين مؤقت يمنع إعادة تحميل الـ 8MB في كل زيارة، مع fallback للـ fetch
- Service Worker متقدم: تخزين الأصول الثابتة وتحديث الكاش بذكاء، يدعم حتى 200MB للصوت
- تطبيع عربي دقيق: normalizeArabic() تزيل التشكيل وتوحد الألف والياء والتاء المربوطة في البحث
- تنظيف نص الآيات: cleanAyaText() تزيل رموز الوقف والأحرف الخاصة
- Timeouts دفاعية: في IndexedDB (3s للقراءة، 5s للكتابة) لمنع التجمد
- IIFE في quran.js: المنطق محاط بدالة مستدعاة فوراً لعزل النطاق
- Prompt engineering محكم: تعليمات النظام في Tibyan واضحة ومقيدة للدقة الشرعية
- دعم السحب (Swipe) + مفاتيح الأسهم + تنقل بأزرار

### في الواجهة
- ثلاثة ثيمات متسقة مع CSS variables
- تصميم Glassmorphism وتأثيرات انتقال سلسة
- تصميم متجاوب يدعم شاشات صغيرة حتى 380px
- نظام إشعارات Tooltip على الأزرار
- تسليط الضوء على كلمة "الله" في النص القرآني

---

## مشاكل حرجة يجب معالجتها فوراً

### 1. index.html — ملف عملاق (115KB, 2946 سطر)

```
المشكلة: كل CSS وJS والـ HTML في ملف واحد ضخم.
التأثير: صعوبة الصيانة، بطء تحليل المتصفح، استحالة إعادة الاستخدام.
الحل: فصل CSS إلى index.css، وJS إلى js/index.js
```

### 2. تكرار دوال مهمة

```javascript
// normalizeArabic() معرفة في مكانين مختلفين:
// → js/quran.js السطر 1095
// → index.html السطر 2451
// وكذلك searchPanel logic مكررة السطور 184-222 و 737-776
```

الحل: استخراجها إلى js/utils.js مشترك.

### 3. renderCurrentPage() يبني HTML بنصوص متسلسلة

```javascript
// السطر 1119 في quran.js
html += '<span class="aya-wrap...">'  // بناء string مباشر
```

المشكلة: رغم استخدام escapeHtml() إلا أن هذا النهج أكثر عرضة للخطأ البشري مستقبلاً.
الحل: استخدام document.createElement() كما في بقية الكود.

### 4. API Key يُرسَل في الـ Headers من المتصفح

```javascript
// index.html السطر 2165
headers: getApiKeyHeaders()  // يرسل مفتاح المستخدم للسيرفر!
```

الخطر: مفتاح Gemini API مكشوف في الـ Network tab لأي شخص.
الحل المثالي: الـ API Key يجب أن يكون في .env على السيرفر فقط، ولا يُرسل أبداً من المتصفح.

### 5. innerHTML في renderSearchResults() بدون تعقيم

```javascript
// index.html السطر 2559
searchResultsList.innerHTML = results.map(v => `
    ...سورة ${v.sura_name_ar}...  ← بيانات من JSON مباشرة في innerHTML
`).join('');
```

الحل: استخدام document.createElement() أو escapeHtml() على القيم.

### 6. moodTafseer و moodSpiritual يستخدمان innerHTML

```javascript
// index.html السطر 2235
document.getElementById('moodTafseer').innerHTML =
    `<strong>التفسير المبسط:</strong> ${data.tafseer}`; // data.tafseer من الـ API مباشرة!
```

الخطر: إذا أعاد الـ AI نصاً يحتوي HTML، سيُنفَّذ مباشرة.
الحل: sanitizeHtml(data.tafseer) قبل الحقن.

---

## تحسينات مهمة (Medium Priority)

### 7. البحث في quran.js يفحص 604 صفحة كاملاً بدون Web Worker

```javascript
// السطر 277 — يتكرر على كل الآيات (6236 آية)
for (let p = 1; p <= 604; p++) {
    const pageVerses = pagesMap[p] || [];
    // ...
}
```

الحل: نقل البحث لـ Web Worker لمنع تجميد الواجهة.

### 8. Service Worker: قائمة الأصول تحتوي تكرارات

```javascript
// sw.js السطور 51-61
'/img/arabesque.png',  // مكررة مرتين!
'/img/app-logo.png',   // مكررة مرتين!
'/img/app-logo1.png',  // مكررة مرتين!
// ... إلخ
```

الحل: تنظيف القائمة وإزالة المكررات.

### 9. name في package.json مضلل

```json
"name": "react-example"  // لكن التطبيق لا يستخدم React!
```

يجب تغييره إلى "khuta-quran" أو اسم مناسب.

### 10. quran_v6_reset_done — منطق Reset متروك في الكود

```javascript
// quran.js السطر 22
if (!localStorage.getItem('quran_v6_reset_done')) {
    localStorage.removeItem('quran_favorites');
}
```

هذا كود مؤقت لمرحلة انتقالية ويجب حذفه.

### 11. handleButtonClick معرفة داخل initData()

```javascript
// quran.js السطر 1473
function handleButtonClick(callback, event) { ... }
```

معرفة داخل async function مما يجعلها غير مرئية خارجياً وصعبة الاختبار.

### 12. Font-family متضاربة

في index.html:
- .body → Arial, Helvetica, sans-serif
- .tibyan-msg → Arial

في quran.css:
- body → 'Noto Naskh Arabic', 'Tajawal', serif

الحل: تعريف --font-ui و --font-quran كـ CSS variables موحدة.

---

## توصيات تحسين الأداء

### 13. تحميل JSON ضخمة بدون Streaming

```javascript
const res = await fetch('assets/quran.json');
data = await res.json();  // 2.5MB في الذاكرة دفعة واحدة
```

الحل المقترح: تقسيم quran.json حسب الجزء (30 ملف صغير) وتحميل الجزء الحالي فقط عند الحاجة. هذا يُسرع التحميل الأول بنسبة 97%.

### 14. الصور غير محسّنة

لا يوجد استخدام لصيغة .webp في الأيقونات، ولا loading="lazy" على الشعار.

### 15. Inline SVG بدلاً من Lucide.js لأيقونات التنقل

```html
<script src="assets/js/lib/lucide.min.js"></script>
```

يُحمّل مكتبة أيقونات كاملة للحصول على 10 أيقونات فقط في الواجهة الرئيسية.
الحل: استخدام Inline SVG للأيقونات الثابتة، والإبقاء على Lucide للأيقونات الديناميكية.

---

## توصيات الواجهة والتصميم

### 16. صفحة الرئيسية (index.html) تفتقر إلى Identity قوية

الصفحة الرئيسية بسيطة جداً — شعار، عنوان، بطاقتان. يمكن جعلها أكثر حيوية بإضافة:
- آية اليوم تُعرض بشكل بارز عند الفتح
- إحصائيات (عدد الآيات المقروءة، الأيام المتتالية)
- تحية ديناميكية بحسب وقت اليوم (صباح الخير، مساء النور...)

### 17. ثيم الـ Dark Mode في index.html غير مطبّق

quran.css تدعم [data-theme="dark"] لكن صفحة index.html لا تطبق الثيم عليها، تظل دائماً فاتحة.

### 18. مشغل الصوت (AudioPlayer) يعتمد على innerHTML كبير

```javascript
// AudioPlayer.js السطر 55
container.innerHTML = `...`  // تقريباً 100 سطر HTML كـ template string
```

هذا يصعّب التعديل والصيانة.

### 19. عدم وجود Animation للتنقل بين الصفحات في Mobile

عند الانتقال من الصفحة الرئيسية لصفحة القراءة لا توجد Transition Animation.

### 20. أزرار التنقل ليس لها Keyboard Shortcut مرئي

يدعم الكود ArrowRight/ArrowLeft لكن لا يوجد إشارة بصرية في الواجهة.

---

## أفكار إبداعية مقترحة

### الفكرة الأولى: "ورد الفجر" — تذكير ذكي

نظام يقرأ وقت الصلاة المحلي ويُرسل Push Notification للمستخدم:
"وقت الفجر — ابدأ وردك اليومي، تبقى X صفحات لتكمل الجزء"

```javascript
// باستخدام Web Push API + Service Worker
self.registration.showNotification('ورد الفجر', {
    body: `ابدأ قراءتك من سورة ${suraName} — صفحة ${page}`,
    icon: '/img/app-logo1.png',
    actions: [{ action: 'open', title: 'ابدأ القراءة' }]
});
```

---

### الفكرة الثانية: "لوحة ختمتي" — تتبع الختمات

صفحة مستقلة تعرض:
- دائرة Radial Progress تُظهر نسبة إكمال القرآن
- Timeline بختمات سابقة مع تواريخها
- أطول سلسلة يومية (Streak بارز)
- خريطة حرارية كـ GitHub Contribution Graph

---

### الفكرة الثالثة: "تحدي آية" — التلاوة والحفظ

ميزة تقترح آية جديدة يومياً للحفظ:
- عرض الآية
- زر "تمرين الحفظ" يُخفي كلمات عشوائية ويطلب من المستخدم إكمالها
- تقييم مدى إتقان الحفظ
- إشعار في اليوم التالي لمراجعة ما حُفظ

---

### الفكرة الرابعة: "مجلس القراءة" — قراءة جماعية

إضافة غرفة قراءة مشتركة مبنية على WebRTC أو BroadcastChannel API:
- يدعو المستخدم أصدقاءه برابط
- يُزامَن موضع الصفحة بين جميع المشاركين
- كل شخص يرى "أين وصل الآخرون" بمؤشرات ملونة

---

### الفكرة الخامسة: "رتّل معنا" — مقارنة التلاوة بالذكاء الاصطناعي

يسجّل المستخدم صوته وهو يتلو، ثم:
- يُحوّل الصوت لنص عبر Web Speech API
- يقارنه بنص الآية من القرآن
- يحسب نسبة الدقة ويبيّن مواضع الاختلاف
- تحدي متقدم: تحليل مخارج الحروف عبر Gemini

---

### الفكرة السادسة: "مصحف الأسرة" — مزامنة بين الأجهزة

استخدام Supabase أو Firebase لمزامنة:
- الإشارة المرجعية (Bookmark) بين أجهزة المستخدم
- المفضلة المشتركة للأسرة (Family Group)
- تقدم الأبناء اليومي مرئي للوالدين

---

### الفكرة السابعة: "بطاقة الآية" — Share على السوشيال ميديا

عند مشاركة آية، يُنشئ التطبيق تلقائياً صورة جاهزة للمشاركة:
- خلفية إسلامية جميلة (مولّدة برمجياً بـ Canvas)
- نص الآية بخط عربي جميل
- اسم السورة والآية وشعار التطبيق
- أحجام جاهزة لـ Instagram Story, Post, WhatsApp

```javascript
// باستخدام Canvas API
const canvas = document.createElement('canvas');
const ctx = canvas.getContext('2d');
// رسم الخلفية والنص...
canvas.toBlob(blob => navigator.share({ files: [new File([blob], 'aya.png')] }));
```

---

### الفكرة الثامنة: "رحلة قرآنية" — Onboarding تفاعلي

عند الفتح الأول:
1. سؤال: "كم تريد أن تقرأ يومياً؟" (slide بصري بثلاثة خيارات)
2. سؤال: "هل تريد تذكيراً يومياً؟"
3. تحديد خطة ختمة (شهر، 3 أشهر، 6 أشهر، سنة)
4. شاشة "انطلق!" تأخذه مباشرة للصفحة الأولى

---

### الفكرة التاسعة: "تبيان البصري" — فهم الآية كـ Infographic

بدلاً من نص التفسير العادي، يُنشئ Gemini لكل آية:
- خريطة ذهنية مصغرة تعرض: الموضوع الرئيسي، الكلمات المفتاحية، الصلة بآيات أخرى
- معروضة كـ Visual Card قابلة للتوسيع

---

### الفكرة العاشرة: "آية الشهر بصوتك"

مسابقة شهرية:
- تُحدَّد آية الشهر
- يسجّل المستخدمون أصواتهم ويرفعونها
- التصويت يكون داخل التطبيق
- الفائز يُعلَن وتلاوته تُعرض للجميع لمدة شهر

---

## هيكل ملفات مقترح

```
الجذر/
├── index.html              ← بسيط، يحمل فقط HTML الهيكلي
├── css/
│   ├── index.css           ← NEW: CSS الصفحة الرئيسية (مفصول)
│   ├── quran.css           ← موجود
│   └── fonts-quran.css     ← موجود
├── js/
│   ├── utils.js            ← NEW: دوال مشتركة (normalizeArabic, escapeHtml...)
│   ├── index.js            ← NEW: منطق الصفحة الرئيسية (مفصول)
│   ├── quran.js            ← موجود (مع تنظيف)
│   ├── AudioPlayer.js      ← موجود
│   ├── AudioService.js     ← موجود
│   └── db.js               ← موجود
├── assets/
│   ├── quran/              ← NEW: quran.json مقسم حسب الجزء
│   │   ├── juz_01.json
│   │   ├── ...
│   │   └── juz_30.json
│   └── tafseer.json        ← موجود
├── server/app.ts           ← موجود
├── sw.js                   ← موجود (مع تنظيف)
└── manifest.json           ← موجود
```

---

## ملخص التقييم

| المجال | التقييم | الملاحظة |
|--------|---------|-----------|
| الأمان (XSS) | 4/5 | ممتاز، مع نقطتي ضعف بسيطتين |
| الأداء | 3/5 | جيد، لكن JSON ضخمة وبحث بلا Worker |
| بنية الكود | 3/5 | مكررات ومزج CSS+JS مع HTML |
| التصميم | 4/5 | راق ومتسق، يحتاج Identity أقوى |
| إمكانية الوصول (A11y) | 2/5 | يفتقر لـ ARIA labels وروابط تخطي |
| SEO | 3/5 | meta description موجودة، يحتاج Structured Data |
| الابتكار | 5/5 | فكرة تبيان AI فريدة ورائعة |

---

## خارطة الطريق المقترحة

### المرحلة الأولى — إصلاح (أسبوعان)
1. فصل CSS و JS من index.html
2. إنشاء js/utils.js لإزالة التكرارات
3. تأمين API Key (لا إرسال من المتصفح)
4. إصلاح XSS في renderSearchResults
5. تنظيف sw.js من التكرارات
6. تحديث package.json (name, etc.)

### المرحلة الثانية — تحسين (شهر)
7. تقسيم quran.json إلى أجزاء
8. نقل البحث لـ Web Worker
9. تطبيق الثيم على index.html
10. تنفيذ Onboarding (الفكرة الثامنة)
11. إضافة "آية اليوم" للصفحة الرئيسية

### المرحلة الثالثة — ابتكار (شهران)
12. "بطاقة الآية" للمشاركة (Canvas API)
13. "لوحة ختمتي" الإحصائية
14. "تحدي آية" للحفظ
15. Push Notifications لورد الفجر

---

## خاتمة

مصحف خطى مشروع رائع ومحفوف بالمحبة والإتقان — يظهر ذلك جلياً في الإهداء القرآني المؤثر وفي الاهتمام بتجربة المستخدم. الأساس متين والبنية واعدة.

التوصيات المذكورة ليست انتقاداً بل هي خطوات للارتقاء بهذا العمل الصالح إلى مستوى التطبيقات القرآنية العالمية.

"وَمَا تَوْفِيقِي إِلَّا بِاللَّهِ" — هود: ٨٨

---
أُعد هذا التقرير بعد مراجعة شاملة لأكثر من 8 ملفات وأكثر من 10,000 سطر من الكود.
